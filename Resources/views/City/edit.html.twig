{% extends 'LthrtCCSBundle:City:city.html.twig' %}

{% block body -%}
    <h1>City Edit</h1>
    {% include 'LthrtCCSBundle:City:form.html.twig' with { 'submitLabel' : 'Update' } %}
    <ul class="record_actions">
    <li>
        <a href="{{ path('city_list') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock body %}
